import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(
    0xFF2C3E50,
  ); // Un azul oscuro para elementos principales
  static const Color backgroundColor = Color(
    0xFF1E2732,
  ); // Fondo oscuro general
  static const Color cardColor = Color(
    0xFF2B3A4A,
  ); // Color de las tarjetas de publicaciones
  static const Color textColor = Color(0xFFECF0F1); // Texto claro
  static const Color secondaryTextColor = Color(
    0xFFAAB8C2,
  ); // Texto secundario (horas, likes, etc.)
  static const Color iconColor = Color(0xFFECF0F1); // Iconos claros
  static const Color accentColor = Color(
    0xFF3498DB,
  ); // Color de acento para botones, etc.
}
